import { Colors } from './constants/Colors';

export const theme = {
    colors: {
        primary: Colors.light.primary,
        background: Colors.light.background,
        text: Colors.light.text,
        gray200: Colors.light.border,
        alert: Colors.light.alert,
        success: Colors.light.success,
        white: '#FFFFFF',
    },
};